{
    "name": "hcm-skills-matrix",
    "private": true,
    "scripts": {
        "build:api": "yarn --cwd backend build:exe",
        "build:front": "yarn --cwd frontend build:desktop",
        "build:win": "yarn build:api \u0026\u0026 yarn build:front"
    }
}